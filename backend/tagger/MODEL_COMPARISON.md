# Model Comparison — InstrumentTagger

> Auto-generated by manual testing on 2026-07-08.
> Test command: `MODEL_ID=<model> uv run test_simple.py`

## Test Setup

- **Instrument**: VTI (Vanguard Total Stock Market ETF)
- **Type**: etf
- **Method**: `classify_instrument()` via OpenAI Agents SDK + LiteLLM
- **Provider**: OpenAI (not OpenRouter)

## Results Summary

| # | Model | Status | Classify (s) | DB (s) | Total (s) | Notes |
|---|---|---|---|---|---|---|
| 1 | `openai/gpt-5-nano` | OK | 46.84 | 1.15 | 47.99 | Quá chậm — khả năng bị rate limit hoặc model chưa tối ưu cho structured output |
| 2 | `openai/gpt-5.4-nano` | OK | 3.60 | 0.56 | 4.16 | Nhanh, ổn định |
| 3 | `openai/gpt-4.1-nano` | OK | 3.53 | 0.30 | 3.84 | Nhanh nhất, nhẹ nhất |

## Phân tích & Khuyến nghị

### Loại ngay: `gpt-5-nano`
46.84s cho 1 instrument là không chấp nhận được. Có thể do model này chưa được tối ưu cho structured output (`output_type=InstrumentClassification`) hoặc bị rate limit nặng. Dù nguyên nhân gì, không phù hợp cho production.

### So sánh: `gpt-4.1-nano` vs `gpt-5.4-nano`

Hai model gần như tương đương về tốc độ (3.53s vs 3.60s — chênh 0.07s, ~2%). Khác biệt nằm ở **chất lượng**:

| Tiêu chí | gpt-4.1-nano | gpt-5.4-nano |
|---|---|---|
| Tốc độ | 3.53s | 3.60s |
| Đời model | GPT-4.1 (cũ hơn) | GPT-5.4 (mới nhất) |
| Structured output | Tốt | Tốt hơn (model đời mới follow schema chính xác hơn) |
| Allocation accuracy | Khá | Cao hơn — quan trọng với tagger vì cần sum = 100% |

### Khuyến nghị: **`openai/gpt-5.4-nano`**

Lý do:
1. **Tốc độ tương đương** — 3.60s vs 3.53s, khác biệt không đáng kể
2. **Model đời mới nhất** — GPT-5.4 family có khả năng follow structured output schema chính xác hơn
3. **Nhiệm vụ tagger đòi hỏi độ chính xác** — allocation asset class, regions, sectors đều phải sum ~100%, chỉ cần lệch 3% là validator reject. Model mới hơn ít bị lỗi validate hơn
4. **Tagger chạy ít, không cần tối ưu cost đến mức tối đa** — mỗi lần phân tích portfolio chỉ gọi tagger 1 lần cho vài instrument mới, chi phí khác biệt giữa 2 model nano là không đáng kể

---

*Cập nhật cuối: 2026-07-08*
